# web-launcher — Agent Instructions

A Claude Code plugin that diagnoses why a live site is not indexed, fixes it, and verifies the fix.

**Read `CLAUDE.md` first.** It is the main context source for this repository: what the project is,
the rules that actually bite, standing assumptions, and where the documentation lives. This file
exists only so that tools looking for `AGENTS.md` land in the right place.

Then read `docs/product/00-state.md` for where the project currently stands and what comes next.
That directory is gitignored, so it will be missing in a clone — in that case `CLAUDE.md` is the
whole context.

Do not duplicate project context into this file. One source, one place to keep current.
